for(let i = '*'; i<=1 ; i++){
    for(let i = '*'; i<=2 ; i++)
        for(let i = '*'; i<=3 ; i++)
            for(let i = '*'; i<=4 ; i++)
                 for(let i = '*'; i<=5 ; i++)
                    for(let i = '*'; i>=4 ; i++)
                        for(let i = '*'; i>=3 ; i++)
                             for(let i = '*'; i>=2 ; i++)
                                for(let i = '*'; i>=1 ; i++)
                        console.log(i);
}

